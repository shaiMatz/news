import { registerRootComponent } from 'expo';
import App from './src/App';

// Register the main App component
registerRootComponent(App);

// Export it as the default export for compatibility
export default App;